# ── Raw data (private until Elisa publishes) ──────────────────────────────
kraken2_family_matrix.tsv
krakenuniq_family_matrix.tsv
metadataGOG.xlsx
family_correlations.tsv
pcoa_coordinates.tsv
family_matrices_report.md

# ── Reports and presentations (personal/draft) ───────────────────────────
*.pages
*.pdf
*.pptx

# ── Shell scripts with server paths / credentials ─────────────────────────
krakenuniq.sh
krakenuniq_batch.sh
download_matrices.sh

# ── Misc output / temp ────────────────────────────────────────────────────
*.Rout
*.RData
*.rds
.Rhistory
.RData

# ── macOS ─────────────────────────────────────────────────────────────────
.DS_Store

# ── Folders to exclude ────────────────────────────────────────────────────
intersezione/
tutti/
training/
application/
